# Deploy with --cpu-always-allocated (gcloud run deploy --no-cpu-throttling) so
# the background drift-eval thread survives after the 202 response.
web: functions-framework --target=eval_drift --port=$PORT
